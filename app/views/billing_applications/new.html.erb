<%= render 'side_bar/daily_office_side_bar' %>
<%= form_for @billing_application do |f| %>
  <% if @billing_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billing_application.errors.count, "error") %>
        &nbsp;prohibited this user from being saved:</h2>
      <ul>
        <% @billing_application.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <h3><%= I18n.t 'views_billing_application.title' %></h3>
  </p>

  <p>
    <label for="item_name">
      <%= I18n.t 'activerecord.attributes.billing_application.purchase_company'%>
    </label><br/>
    <%= f.text_field :purchase_company, :size => "60" %>
  </p>

  <p>
    <label for="use">
    	<%= I18n.t 'activerecord.attributes.billing_application.use'%>
    </label>
    <br />
    <%= render 'application/use_radio', :f => f, :use_names => BillingApplication::USE%>
  </p>

  <p>
    <label for="sum">
    	<%= I18n.t 'activerecord.attributes.billing_application.sum'%>
    </label><br/>
    <%= f.text_field :sum, :size => "5" %>
  </p>

  <p>
    <label for="reason"><%= I18n.t 'activerecord.attributes.billing_application.reason'%>
    </label><br/>
    <%= f.text_area :reason, :size => "60x3" %>
  </p>

  <p><%= submit_tag I18n.t('operate.commit') %></p>
<% end %>

<%= link_to I18n.t('operate.back'), billing_applications_path %>
